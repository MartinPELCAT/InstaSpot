import { Box, Typography } from "@material-ui/core";
import React, { FunctionComponent } from "react";

export const NewPlaces: FunctionComponent = () => {
  return (
    <Box marginY={2}>
      <Typography variant="h5">New Places</Typography>
    </Box>
  );
};
